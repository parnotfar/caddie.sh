# Caddie.sh Version Management
# This file provides encapsulated version management functions

source "$HOME/.caddie_modules/.caddie_cli"

# Private version variable
CADDIE_SH_VERSION="5.1.0"

# Function to get the current version
function caddie_version_get() {
    caddie cli:blue "$CADDIE_SH_VERSION"
}

# Function to set a new version
function caddie_version_set() {
    local new_version="$1"
    if [ -n "$new_version" ]; then
        CADDIE_SH_VERSION="$new_version"
        caddie cli:check "Caddie version set to: $CADDIE_SH_VERSION"
    else
        caddie cli:red "Error: Please provide a version number"
        return 1
    fi
}

# Function to reset version to default
function caddie_version_reset() {
    CADDIE_SH_VERSION="1.0"
    caddie cli:check "Caddie version reset to: $CADDIE_SH_VERSION"
}

# Function to show current version
function caddie_version_show() {
    caddie cli:blue "Caddie.sh version: $CADDIE_SH_VERSION"
}

# Export functions for external use
export -f caddie_version_get
export -f caddie_version_set
export -f caddie_version_reset
export -f caddie_version_show
